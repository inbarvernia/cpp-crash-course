#include <iostream>

int main() {

  // For the sake of completion, the remaining storage classes which will not be discussed in detail here:
  // Mutable storage class (to do with OOP)

  // thread_local storage class (introduced around C++11)

  return 0;
}